Drop your screen-recorded clips here, named exactly:

  smite.mp4    — lazy prayer → lightning smite (the hook)
  temple.mp4   — /temple: faucet → approve → enter → Disciple card + check-in
  hero.mp4     — /oracle-world: FIND MY HERO → your glowing hero in the world
  bless.mp4    — sincere prayer → golden blessing + Mantlescan USDY transfer
  proof.mp4    — /prophecies + Mantlescan recent txns (postSnapshot/postProphecy)
  mantle.mp4   — Mantlescan: click a tx → highlight the sub-cent gas fee

Record at 1920x1080, 60fps (OBS). The composition crops to fill, so framing
is forgiving. Until a clip exists, that scene previews as a placeholder.

Optional: drop public/music.mp3 and uncomment the <Audio> line in DemoVideo.tsx.
